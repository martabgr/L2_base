function primeFactors(n){
  let res='';
  for(let i=2;i<=n;i++){
    let count=0;
    while(n%i==0){
      count ++;
      n/=i;
    }
    if(count>0){
    if(count==1){
      res+=`(${i})`;
    }
      else{
        res+=`(${i}**${count})`;
      }
    }
  }
  return res;
}